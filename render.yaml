services:
  - type: web
    name: e-commerce-recommendation-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn src.api.fastapi_app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
